import { FunctionComponent } from 'preact';

interface InputMaskProps {
    letters: number;
}

const InputMask: FunctionComponent<InputMaskProps> = ({ letters = 3 }) => {
    const href = '';
    return <input />;
};

export default InputMask;
